
function selectPatron(PID) {
    // add PID of selected patron to div for formatting and
    // add PID as form value
    selection = document.querySelector('.selectedPatron');
    selection.innerText('test');
}
